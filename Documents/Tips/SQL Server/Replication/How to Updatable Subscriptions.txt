(未完成)

■手順
○トランザクション レプリケーション (更新可能なサブスクリプション、キュー更新)
・準備
主データベース一式を配置する。
副データベースを作成する (テーブルは不要)。
ReplData フォルダーに対し、SQL Server Agent の実行アカウントである [NETWORK SERVICE] にフルコントロールを許可。

・パブリケーションの作成
[レプリケーション] → [新規作成] → [パブリケーション]

・サブスクリプションの作成
[レプリケーション] → [新規作成] → [サブスクリプション]
[変更をキューに登録し、可能な場合はコミットする]

・パブリケーションのプロパティ
競合した場合、パブリッシャを保持
